from utilities.console import console
from ..simulation.simulation_manager import simulation_manager

# def frame_changed_post(scene):
#     console.info("Frame changed post")
#
#
# def frame_changed_pre(scene):
#     console.info("Frame changed pre")


# def depsgraph_update_post(scene, depsgraph):
#     console.info("depsgraph_update_post")
    # # for obj in simulation_manager.simulated_objects:
    # #     obj = obj['obj']
    # #     console.info(obj.name_full, obj.type, obj.data,obj.is_evaluated)
    #
    # for update in depsgraph.updates:
    #     _id = update.id
    #     obj = update.id.original
    #     if hasattr(obj, 'type'):
    #         console.info(_id, obj.type, obj.data)

